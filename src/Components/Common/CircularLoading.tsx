import React from "react";

import { CircularProgress } from "@mui/material";

function CircularLoading() {
  return <CircularProgress sx={{ color: "rgba(0, 0, 0, 0.5)" }} />;
}

export default CircularLoading;
